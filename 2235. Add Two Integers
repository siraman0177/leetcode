int sum(int num1, int num2) {
    int sum=0;
    sum =num1+num2;
    return sum;
    
}
//a very simple code ...google code 1
// same code in python 

class Solution:
    def sum(self, num1: int, num2: int) -> int:
        sum=0
        sum=num1+num2
        return sum
        
